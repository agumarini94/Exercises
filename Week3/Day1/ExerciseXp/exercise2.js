const colors = ["red", "blue", "green", "orange"];
const posiciones = ['1st', '2nd', '3rd', '4th']
    for (let i = 0; i < colors.length; i++) {
        console.log('My ' + posiciones[i] + 'choice is ' + colors[i]);
    }
