// Escriba una función formatValueque tome un parámetro de tipo alphanumeric(a stringo a number) y realice lo siguiente:

// Si el parámetro es un número, devuelve el número formateado como moneda(por ejemplo, $100.00).
// Si el parámetro es una cadena, devuelve la cadena en mayúsculas.
// Si el parámetro no es una cadena ni un número(puede simular este escenario), devuelva el mensaje "Unsupported type".
// Implemente la formatValuefunción que utiliza el typeofoperador para determinar el tipo y manejar la entrada en consecuencia.


function formatValue(value: alphanumeric): string {
  if (typeof value === "number") {
    return `$${value.toFixed(2)}`; // ---> toFixed es una forma de convertir decimales, coun una cantidad fija de decimales, en este caso, 2 decimales.
  }

  if (typeof value === "string") {
    return value.toUpperCase();
  }

  return "Unsupported type";
}

// Example usage:
console.log(formatValue(150));  // Output: "$150.00"
console.log(formatValue("hello"));  // Output: "HELLO"





// type alphanumeric = string | number;

// function formatValue(a: alphanumeric): alphanumeric {
//   if (typeof a === "number") {
//     return `$${a}`;
//   }

//   if (typeof a === "string") {
//     return a.toUpperCase()
//   }

//     throw new Error ('Unsupported type');
// }
